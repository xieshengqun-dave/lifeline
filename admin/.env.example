# Copy to .env.local to override the backend URL for your machine.
# The app already defaults to the same LAN IP the patient/operator apps use
# (see src/api/client.js), so this file is optional for local dev.
VITE_API_BASE_URL=http://192.168.100.133:4000
